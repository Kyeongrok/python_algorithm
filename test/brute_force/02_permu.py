
def fn():
    pass

lst = ['A', 'B', 'C']
# A와 B와 C를 모두 돌립니다.
for i in range(len(lst)):
    print(lst[i])
    # A를 뽑았으면 B, C를 가지고 무언가 만들어야 합니다.
    # 그리고 하나가 남을때까지 반복 해야 합니다
    # 하나씩 줄여서 했다면 하나가 끝나고 다시 올라와야 합니다.